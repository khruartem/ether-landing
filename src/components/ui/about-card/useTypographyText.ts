import { createUseTypography } from "../../../utils/typographyHookFactory";

export const useTypographyText = createUseTypography("Text", 400, {
  large: 18,
  desktop: 14,
  laptop: 14,
  tablet: 14,
  mobile: 14,
});
