import { createUseStyleHook } from "../../../utils/styleHookFactory";

export const useStyle = createUseStyleHook({
  gap: {
    large: 20,
    desktop: 20,
    laptop: 20,
    tablet: 16,
    mobile: 16,
  },
});
