import { createUseStyleHook } from "../../../utils/createUseStyleHook";

export const useStyle = createUseStyleHook({
  paddingBlock: {
    large: 20,
    desktop: 12,
    laptop: 12,
    tablet: 12,
    mobile: 12,
  },
});
